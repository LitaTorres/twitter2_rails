<p style="color: green"><%= notice %></p>

<h1> Inicio </h1>

<div class="inicio row">
  <div class="col-sm-6 mb-3 mb-sm-0">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Para ti</h5>
        <p class="card-text"> ¡Porque tú nos importas! Revisa estas sugerencias especiales para ti</p>
        <a href="#" class="btn btn-primary">Revisar </a>
      </div>
    </div>
  </div>
  <div class="col-sm-6">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Siguiendo</h5>
        <p class="card-text"> Mira las tendencias de tus seguidores.</p><br>
        <a href="#" class="btn btn-primary">Seguidores</a>
      </div>
    </div>
  </div>
</div>
<br> <br>
<div class="d-flex justify-content-center">
  <%== pagy_bootstrap_nav(@pagy) %> <!-- no olvidar para mostrar la paginación-->
</div>

<!-- 
<h1>Tweets</h1> 
--agregando ahora este formulario que es un buscador luego modif el controlador para q esto funcione ESTO LO PUSE EN EL LAYOUTS--
<%= form_with url: tweets_path, method: :get do |form| %>
    <%= form.text_field :query_text, value: params[:query_text] %>
    <%= form.submit "Buscar", class: "btn btn-primary" %>
<% end %>  <br>  <br> 
-->

<div id="tweets">
<!--modificando con if else end por si no existen coincidencias -->
  <% if @tweets.count > 0 %> <!-- linea agregada al final x si no existen coincidencias-->
    <% @tweets.order(id: :desc).each do |tweet| %> <!--acá le agregué lo del older id desc para que el ultimo tweet se vea de los 1ros-->
      <%= render tweet %>
      <p>
        <button class="btn btn-info btn-lg" type="submit">
        <%= link_to "Ver más...", tweet, class:"textotwitter2" %> </button>
      </p>
    <% end %>
    


  <% else %> <!-- en caso contrario -->
      <h1> No existen coincidencias </h1>
  <% end %>

</div>

