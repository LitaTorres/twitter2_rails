<%= form_with(model: tweet) do |form| %>
  <% if tweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_name, "Nombre de usuario", style: "display: block" %> <br> 
    <%= form.text_field :user_name %> <br> <br>
  </div>

  <div>
    <%= form.label :description, "Cuentanos lo que estás pensando", style: "display: block" %>  <br> 
    <%= form.text_area :description, rows: 5, cols: 70 %> <br> <br>
  </div>
  
  <div>
    <%= form.label :url_image, "Pega la url de la imagen que quieras", style: "display: block" %>  <br> 
    <%= form.text_field :url_image %> <br> <br>
  </div>

  <div>
    <%= form.submit "Enviar", class: "btn btn-primary" %>
  </div>
<% end %>
